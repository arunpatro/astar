# Path planning for controlling a crane

Using afine and perspective transform, first reshape the image and then binarize it using `binarize.py`. 

Using `main.py`, obtain the shortest path using A* Star Algorithm.
